webpackJsonp([0xa1dd11c14917],{919:function(e,a){e.exports={data:{markdownRemark:{html:'<blockquote>\n<p>JavaScript suffers and imposes Decision Fatigue on developers. I am enjoying working with <code class="language-text">React</code> and <code class="language-text">Express</code>, but the search for better tools and techniques is exhausting!</p>\n</blockquote>\n<p>JavaScript [under the <code class="language-text">Node.js</code> ("<code class="language-text">Node</code>") ecosystem] has a reasonably well-known limitation: it is prone to cause decision fatigue.</p>\n<p>Over the past month or so of free-form investigation, I have juggled the following technologies or tools:</p>\n<ul>\n<li><code class="language-text">Typescript</code></li>\n<li>TDD <code class="language-text">Jest</code> (<code class="language-text">Chai</code>/<code class="language-text">Mocha</code> were considered, but I think <code class="language-text">Enzyme</code> may better-suit my needs, and <code class="language-text">Enzyme</code> seems to best-pair with <code class="language-text">Jest</code>)</li>\n<li>Multiple CI build/configuration/manipulation scripts</li>\n<li>Different code coverage tools (e.g. CodeClimate, <code class="language-text">inferno-scripts test --env=jsdom --coverage</code>)</li>\n<li><code class="language-text">MobX</code> (I tend to use <code class="language-text">Redux</code>, but I\'m exploring <code class="language-text">MobX</code> with <code class="language-text">Inferno</code> in building a <code class="language-text">ResumeBuilder</code>)</li>\n<li><code class="language-text">GatsbyJS</code> with accompanying tools <code class="language-text">Apollo</code> and <code class="language-text">GraphQL</code></li>\n<li>Various <code class="language-text">Functional Programming</code> techniques--I know the theory behind these (thanks, misspent academic youth!), but something like <code class="language-text">Elm</code> still makes me worry about how I\'ll run this through <code class="language-text">Babel</code> to polyfill and present to <code class="language-text">Webpack</code></li>\n<li>Performance optimization--how do I <em>definitively</em> prove, aside from either running timing-scripts in the CLI or looking at Chrome\'s debugging console, that the apps I write are production-ready?</li>\n</ul>\n<p>This list, while exhausting, is <em>not</em> exhaustive. I have worked through many technical stacks in my time, and I really like the <code class="language-text">Node</code>-based stack It has good infrastructure, reasonable-seeming performance (text-file parsing aside--<code class="language-text">Python</code> beats the daylights out of <code class="language-text">JavaScript</code> for this), easy deployment out to <code class="language-text">Heroku</code> and friends, and an active community. Unfortunately, there is something of a paradox of choice here, and this leads to decision-fatigue. </p>\n<p>As an example, let\'s look at a front-end app that runs in the browser and delivers some sort of web content. You want to build this in <code class="language-text">JavaScript</code>. What are your viable options under <code class="language-text">Node</code>?</p>\n<ul>\n<li><code class="language-text">React</code></li>\n<li><code class="language-text">Preact</code> (<code class="language-text">React</code> with SSR)</li>\n<li><code class="language-text">Inferno</code> (<code class="language-text">React</code> with SSR options and lack of synthetic event handling)</li>\n<li><code class="language-text">Angular</code></li>\n<li>Others (<code class="language-text">Ember</code>, <code class="language-text">Meteor</code>, <code class="language-text">Vue</code>, <code class="language-text">NextJS</code>...)</li>\n</ul>\n<p>There are lots of options here!</p>\n<p>Each of these carries decisions in their own right:</p>\n<ul>\n<li>Testing</li>\n<li>Typescript or Flow for typing--or nothing?</li>\n<li>Linting (<code class="language-text">ESLint</code>) and/or Polyfill (<code class="language-text">Babel</code>)</li>\n<li>JS <code class="language-text">callback</code>s, <code class="language-text">Promise</code>s, or <code class="language-text">async</code>/<code class="language-text">await</code> asynchronous call resolution (or <code class="language-text">yield</code>, I haven\'t worked with <code class="language-text">yield</code> in <code class="language-text">JavaScript</code> yet)</li>\n</ul>\n<p>At least the back-end/server-side of this environment is simpler; we really have two solid choices:</p>\n<ul>\n<li><code class="language-text">Express</code></li>\n<li><code class="language-text">Koa</code></li>\n</ul>\n<p>But these two choices themselves carry much of the above decisions ("How do I test this?" "Do I write strongly-typed code that will compile to <code class="language-text">JavaScript</code>, or do I just write raw <code class="language-text">JavaScript</code> and be careful about typing?" "Do I want to enable <code class="language-text">ES2015</code> syntax?" [<code class="language-text">Express</code> only; <code class="language-text">Koa</code> builds this in])</p>\n<p>While I tend to work primarily in <code class="language-text">React</code> [or "<code class="language-text">React</code>-inspired"] and <code class="language-text">Express</code> for front- and back-end work, as a long-time developer who is also fairly new to digging into the <code class="language-text">JavaScript</code> full-stack infrastructure and associated tools, I am always left with a nagging thought: <em>is what I\'m building the best way to go? Am I going to get caught out on performance when I go to scale this?</em> These are questions that people new to development as a whole won\'t even know to ask, and I wonder how frequently this catches up with us, and how much of this is due to having entirely <em>too many options</em>.</p>',frontmatter:{date:"2018-03-12T14:11:57-07:00",path:"/blog/2018-03-12_javascript-and-decision-fatigue",title:"JavaScript and Decision Fatigue"}}},pathContext:{}}}});
//# sourceMappingURL=path---blog-2018-03-12-javascript-and-decision-fatigue-55394a81ad7d7f10c694.js.map